package ch.circadia.tracker

import org.junit.Test
import org.junit.Assert.*

class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }

    @Test
    fun force_fail() {
        fail("Checking if tests are running at all")
    }
}
